import React from "react";

const Home = () => {
 
    return (
      <h1 class="display-3">Welcome to World Heritage Site Reviews</h1> //no need to use <div> as this contains only one element fragment of <h1>
      );
  };

  export { Home }